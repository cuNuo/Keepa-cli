# 真实 Keepa 低成本 smoke

## 背景

用户说明已配置真实 Keepa token，并要求开始测试，但不要测试大批量。按低成本原则，仅执行只读诊断与单 ASIN live 请求，不执行 finder、deals、topsellers、batch、tracking 写入或其他可能放大 token 消耗的命令。

## 执行记录

- `.\.venv\Scripts\python.exe -m keepa_cli --json doctor`
  - 结果：成功。
  - 认证：`auth.available=true`，`auth.source=config`。
  - 安全性：输出未包含明文 token。
- `.\.venv\Scripts\python.exe -m keepa_cli --json products get B001GZ6QEC --domain US --history 0`
  - 结果：成功。
  - 请求：GET `/product`，`domain=1`，`asin=B001GZ6QEC`，`history=0`。
  - token：`tokens_consumed=1`，`tokens_left=78`，`refill_rate=60`。
  - 数据：返回 1 个产品，标题为 `Dark Knight, The: 2-disc BD`。

## 结论

真实 config token 可用；Keepa live 读请求链路、请求构造、响应解析、token bucket 元数据和脱敏输出均可工作。本轮未执行批量、高成本或写入类命令。

## 单 ASIN 返回信息摘要

### 产品身份

- ASIN：`B001GZ6QEC`
- 标题：`Dark Knight, The: 2-disc BD`
- 品牌：`WARNER BROS`
- 制造商：`WarnerBrothers`
- 型号/部件号：`BR1000026387`
- 商品类型：`PHYSICAL_MOVIE`
- 关键词类型：`dvd-players`
- URL slug：`Dark-Knight-BD-Live-Blu-ray`
- 版本：`Special Edition`
- 格式：`ntsc`
- 装订：`blu_ray`
- 颜色：`color`
- UPC：`085391176572`
- EAN：`0085391176572`
- GTIN：`00085391176572`

### 分类与榜单

- rootCategory：`2625373011`
- categoryTree：`Movies & TV -> Movies`
- categories：`2649512011`
- salesRankDisplayGroup：`dvd_display_on_website`
- salesRankReference：`2625373011`
- salesRanks：
  - `2625373011`：`6915`
  - `2959126011`：`1080`
  - `2959130011`：`828`
  - `2959144011`：`84`
  - `917972`：`-1`

### 价格、销售与供给

- competitivePriceThreshold：`949`
- monthlySold：`50`
- availabilityAmazon：`0`
- buyBoxEligibleOfferCounts：`[8, 41, 17, 91, 0, 2, 0, 0]`
- offersSuccessful：`false`
- lastPriceChange：`8075720`
- lastUpdate：`8075720`
- lastSoldUpdate：`8065736`
- lastRatingUpdate：`8075528`
- trackingSince：`119040`

### 费用、履约与尺寸

- referralFeePercent：`15`
- referralFeePercentage：`15.03`
- variableClosingFee：`180`
- FBA pickAndPackFee：`258`
- packageWeight：`68`
- packageLength/Width/Height：`170 / 135 / 12`
- itemWeight：`68`
- itemLength/Width/Height：`184 / 140 / 13`
- isEligibleForSuperSaverShipping：`true`
- isEligibleForTradeIn：`true`
- isAdultProduct：`false`
- isHeatSensitive：`false`

### 内容与媒体

- 描述：返回了电影简介，确认描述字段可用。
- feature：`Dc Comics Dark Knight, The (Blu-ray)`
- 图片：
  - large：`918yndApNWL.jpg`，`2000x2529`
  - medium：`51djWQw7e1L.jpg`，`395x500`
- frequentlyBoughtTogether：`B0771GZLZ9`、`B0771RWCH1`
- 语言：
  - English Subtitled
  - French Subtitled
  - Spanish Subtitled
  - English Original Language

### 贡献者

- director：`Christopher Nolan`
- producer：`Benjamin Melniker`、`Christopher Nolan`、`Charles Roven`、`Michael E. Uslan`、`Kevin De La Noy`、`Emma Thomas`、`Thomas Tull`
- actor：`Christian Bale`、`Michael Caine`、`Heath Ledger`、`Gary Oldman`、`Morgan Freeman`、`Maggie Gyllenhaal`、`Aaron Eckhart`
- writer：`Jonathan Nolan`、`David S. Goyer`

### 时间与发布

- publicationDate：`20081209`
- releaseDate：`20081209`
- listedSince：`-1183432`
- lastEbayUpdate：`7935626`

### Keepa 响应与 token 元数据

- processingTimeInMs：`6`
- timestamp：`1778384542514`
- refillIn：`52386`
- refillRate：`60`
- tokensConsumed：`1`
- tokensLeft：`78`
- tokenFlowReduction：`0.0`

## 后续建议

- 下一步若要继续 live 验证，建议仍按“单命令、单资源、可预估 token”的顺序逐项确认。
- 暂不建议直接 live 跑 finder/deals/batch/report 组合；应先用 dry-run 或 fixture 验证参数，再由用户确认具体 live 范围。

## 2026-05-10 继续优化：完整产品详情预设

用户指出应参考 `D:\ProductSelectionSystem` 的 Keepa 请求方式，让 1 token 能尽量拿到完整产品信息。对照发现该项目产品请求默认使用：

- `history=1`
- `stats=180`
- `videos=1`
- `aplus=1`
- 可选 `days`

本项目此前 service 层已支持多数字段，但 CLI 只暴露 `history / stats / update / offers`。本轮补齐：

- `products get --full`：低成本完整详情预设，自动发送 `history=1`、`stats=180`、`videos=1`、`aplus=1`。
- 显式参数：`--days`、`--videos`、`--aplus`、`--rating`、`--buybox`、`--stock`、`--code-limit`、`--only-live-offers`、`--historical-variations`。
- `products get --out <file>`：完整响应很大时把 body 写入 JSON 文件，终端 envelope 仍保留 output 元数据。
- 保守成本边界：`--full` 不自动开启 `offers`、`stock` 等可能显著增加 token 成本的参数。

### dry-run 验证

命令：

```powershell
.\.venv\Scripts\python.exe -m keepa_cli --json products get B001GZ6QEC --domain US --full --dry-run
```

结果：

- endpoint：`/product`
- params：`domain=1`、`asin=B001GZ6QEC`、`history=1`、`stats=180`、`videos=1`、`aplus=1`
- estimated_tokens：`1`
- requires_confirmation：`false`

### live 验证

命令：

```powershell
.\.venv\Scripts\python.exe -m keepa_cli --json products get B001GZ6QEC --domain US --full
```

结果：

- 成功。
- token：`tokensConsumed=1`，`tokensLeft=55`，`refillRate=60`。
- 请求仍为单 ASIN、只读 Product Request，未执行批量或高成本 command family。
- 返回包含完整 `csv` 历史数组、`stats` 汇总、`videos` 列表、分类、贡献者、价格/排名/销售字段、尺寸/费用/履约字段与 token bucket 元数据。

### full 返回中新增确认的信息

- `csv`：已返回大量历史序列数组，证明 `history=1` 生效。
- `stats`：已返回 `current`、`avg30`、`avg90`、`avg180`、`avg365`、`min`、`max`、`salesRankDrops30/90/180/365`、`buyBoxPrice`、`buyBoxSellerId`、`totalOfferCount` 等统计字段。
- `videos`：已返回 10 条视频对象，包含 `title`、`duration`、`image`、`url`、`creator/name`。
- `aplus`：本次样本未观察到可用 A+ 内容字段，判断为商品侧无 A+ 数据或 Keepa 未返回；请求参数已确认发送。
- 终端 UX：完整响应极大，后续正式抓取建议加 `--out .\product-full.json` 保存完整 body。

### 验证命令

- `.\\.venv\\Scripts\\python.exe -m unittest tests.test_service_commands tests.test_cli -v`：30 tests passed。
- `.\\.venv\\Scripts\\python.exe -m keepa_cli --json products get B001GZ6QEC --domain US --full --fixture product_B001GZ6QEC.json --out .\\tmp-product-full.json`：成功，确认 `--out` metadata 与文件写出；临时文件已删除。

## 2026-05-10 继续优化：Agent 产品视图

用户指出 `history` 字段与其他大字段最终要接入 Agent，不应让 Agent 直接消费 Keepa 原始 `csv` / `stats.current` 位置数组。本轮做输出契约优化，默认 raw 输出不变，新增显式 Agent 视图：

- `products get --agent-view` / `--view agent`：把 Product Object 转为稳定摘要。
- `--history-limit <n>`：控制每个历史序列在 stdout 中保留的最近点数。
- `--out <file>` 与 Agent 视图可同时使用：文件保存完整 raw body，stdout 仅返回摘要与 `data.raw.output.path`。
- 新增 `keepa_cli/product_view.py`，避免继续扩大 `service.py`。

### Agent 视图字段

- `identity`：ASIN、标题、品牌、制造商、domain、产品类型、urlSlug、parentAsin 等。
- `category`：root category、category tree、sales rank reference、current rank、salesRanks 摘要。
- `pricing`：current/avg30/avg90/avg180 价格、Buy Box、coupon、promotions。
- `demand`：monthlySold、monthlySoldHistory、salesRankDrops 30/90/180/365。
- `rating`：rating 与 review count，保留 raw value。
- `offers`：total/retrieved/FBA/FBM offer count、buyBoxEligibleOfferCounts、缺货百分比。
- `media` 与 `aplus`：图片、视频、A+ block/module/image/text 样本。
- `content`、`compliance_and_logistics`、`variations`：Agent 做筛选、报告和审计时的常用字段。
- `stats_summary` 与 `history_summary`：按 Keepa 官方 CsvType index 命名，省略完整 raw 数组。
- `raw_field_presence` 与 `raw`：标记原始字段存在性，并指向完整 raw 输出文件或 fixture。

### 本地真实响应解析验证

未再次访问 Keepa；使用上一轮单 ASIN live 响应 `B0D8W1YVBX` 的本地 runtime log 作为 fixture 解析：

- `view=agent_product`，`product_count=1`。
- `pricing.current` 包含 `buy_box_shipping`、`list_price`、`new`。
- `rating.value=4.3`，`review_count=11598`。
- `monthly_sold=100000`。
- `media`：图片 9，视频 11。
- `aplus`：1 个 block，7 个 module，15 张图片。
- `history_summary.series` 能输出 `new`、`sales_rank`、`rating`、`review_count`、`buy_box_shipping`、`new_fba_offer_count`、`new_fbm_offer_count` 等摘要。

### 验证命令

- `.\.venv\Scripts\python.exe -m unittest tests.test_service_commands tests.test_cli tests.test_schema_snapshot -v`：33 tests passed。

### 风险与边界

- Agent 视图只命名 Keepa 官方 CsvType 已明确的位置；后续如果官方新增 csv index，应先更新映射和 snapshot。
- `history export/trend` 仍保持窄序列展开，适合做价格/排名趋势分析；综合产品判断优先使用 `products get --agent-view`。
- 未提交 raw live runtime log，避免把大响应和潜在敏感业务数据纳入版本库。

## 2026-05-10 继续优化：Agent P0/P1/P2 接入增强

用户要求把此前规划的 Agent 使用优化认真落地。本轮不访问真实 Keepa API，只使用 fixture、snapshot 与本地文件验证。

### P0：Agent 视图 profile、字段选择与质量判断

- `products get --agent-view --view summary|research|deal|audit`：按 Agent 场景控制输出粒度。
- `--fields identity,pricing,demand,rating`：覆盖 profile，只返回指定 product section。
- 每个 product 增加 `data_quality`：列出 `present`、`missing`、`confidence` 与 notes。
- 每个 product 增加 `next_actions`：根据缺失字段建议是否补 `--offers 20`、`--rating 1`、`--aplus 1` 或 `--history 1`。

### P1：对比、可解释信号与上下文分块

- `products compare <ASIN...> --view deal`：返回 `view=products_compare` 的统一 rows，适合 Agent 横向对比。
- `selection_signals`：输出 demand、competition、price_stability、content_quality 与 risk_flags，不做黑箱评分。
- `--chunks-dir <dir>`：把 identity、pricing、demand、rating、offers、media、aplus、selection_signals、history_summary 等 section 写入独立 JSON chunk，stdout 返回 manifest。

### P2：schema 文档与 cassette 工作流

- `schema generate --out docs/schema/products.agent-view.schema.json`：从 schema snapshot 生成产品 Agent 视图 shape 文档。
- `cassettes sanitize <input> --out <output>`：提供 CLI 级 cassette 脱敏入口，清理 URL query、header、body 里的 `key/api_key/apikey/token/authorization`。
- 将原有 `scripts/redact_cassette.py` 的脱敏逻辑下沉到 `keepa_cli/cassettes.py`，脚本与 CLI 复用同一实现。

### 验证命令

- `.\.venv\Scripts\python.exe -m unittest tests.test_service_commands tests.test_cli tests.test_capabilities tests.test_project_tools tests.test_schema_docs -v`：45 tests passed。
- 本地 smoke：`products get --agent-view --view summary --fields ...`、`products compare ...`、`products get --chunks-dir ...`、`schema generate`、`cassettes sanitize` 均成功。

### 风险与边界

- `products.compare` 仍复用 `/product`，真实多 ASIN 请求会按 ASIN 数消耗 token；默认测试只走 fixture。
- `next_actions` 是保守建议，不自动执行高成本补请求。
- `--chunks-dir` 会写本地 JSON 文件，调用方应清理临时目录或把它纳入 evidence 管理。

## 2026-05-10 继续优化：Agent 全量时序特征与 B0D8W1YVBX 验证

用户要求再次测试 `B0D8W1YVBX`，并让 Agent 在 `products get --agent-view` 中直接看到历史信息、时序特征和表征变化。本轮做了以下修正：

- `temporal_features` 改为从 Keepa `csv` 全量序列直接计算，不再受 `--history-limit` 展示点数限制；`history_summary.last_points` 仍按 `history_limit` 控制上下文大小。
- `temporal_features.series` 覆盖 `new`、`sales_rank`、`new_offer_count`、`new_fba_offer_count`、`rating`、`review_count` 等关键序列，输出 `point_count`、`duration_days`、首末变化、上一点变化、30/90 天窗口变化、均值、极值、range、波动系数、斜率和趋势方向。
- 修复真实 Keepa live 响应 shape：兼容 `data.products` 与 fixture 的 `data.body.products`，避免 `--agent-view --out` 时 stdout 出现 `product_count=0`。
- `--full` 预设增加 `rating=1`，让 1 token 单品详情尽量带回 rating/review_count。
- 清理 Keepa 负数哨兵值，避免 `-2` 被 Agent 误读为真实 buy box 价格或 offer 数。
- `out_of_stock_percentage` 改为按 CsvType 名称输出 `unit=percent` 的对象，不再暴露原始长数组或错误 currency 单位。
- schema 版本提升到 `2026-05-10.3`，并同步 `docs/schema/products.agent-view.schema.json` 与 snapshot。

### B0D8W1YVBX 真实验证摘要

命令：`products get B0D8W1YVBX --domain US --full --agent-view --view research --history-limit 12 --out <temp>`。

- live 请求成功，`product_count=1`，raw body 已落到临时文件，stdout 保留 Agent 摘要。
- 本次 live 返回 `tokens_consumed=2`，因为 `--full` 现在包含 `rating=1`；如果只要基础 full 可继续评估是否将 rating 做成可选 profile preset。
- Agent 可见核心字段：身份、当前价格、monthlySold、rating/review_count、内容质量、A+、视频、data_quality、next_actions、selection_signals、history_summary 与 temporal_features。
- 真实样例时序覆盖：`new` 价格 129 点、`sales_rank` 2736 点、`rating` 106 点、`review_count` 2953 点。
- 关键特征示例：new 价格全期从 24.99 到 14.99，`change_pct=-40.016`，30 天窗口 `change_pct=-25.0125`，波动系数 `0.1149`；review_count 90 天增加 5528，`change_pct=91.0708`。

### 验证命令

- `.\.venv\Scripts\python.exe -m unittest discover -s tests -v`：149 tests passed。
- `git diff --check`：通过。
- `.\.venv\Scripts\python.exe D:\.codex\hooks\run_relevant_hooks.py --changed-only`：通过。

### 风险与后续

- `rating=1` 会让真实响应 token 消耗从估算 1 变成实际 2；下一步应把 token budget 对 `rating`、`offers`、`buybox` 的附加成本估算得更细。
- 真实 raw 临时文件未纳入 git；如需归档，必须先走 cassette sanitize 或专门脱敏流程。

## 2026-05-10 继续优化：产品 token budget 细化与 rating 默认回退

用户补充说明：rating 一般不启用，Agent 可从网页侧获取评分，因此 `--full` 不应默认触发 `rating=1`。本轮不访问真实 Keepa API，只用 dry-run、fixture 与本地测试验证预算行为。

### 变更内容

- `products get --full` 回退为低成本预设：只自动请求 `history=1`、`stats=180`、`videos=1`、`aplus=1`，不再自动附加 `rating=1`。
- `token_bucket.estimated` 增加 `components` 与 `notes`，让 Agent 能看到预算来源。
- 产品预算拆分为：
  - `base_product`：`1 token * product count`。
  - `rating`：仅显式 `--rating 1` 时按产品级附加成本估算。
  - `buybox`：仅显式 `--buybox 1` 时按产品级附加成本估算。
  - `offers`：按官方 offer page 规则估算，`6 tokens * ceil(offers / 10) * product count`，并叠加基础产品成本。
  - `update_refresh`：`--update 0` 记录为 worst-case，每个产品最多额外 1 token。
- schema 版本提升到 `2026-05-10.4`，同步 snapshot 与 `docs/schema/products.agent-view.schema.json`。

### dry-run 验证摘要

- `products get B0D8W1YVBX --domain US --full --dry-run`：请求参数不含 `rating`，预算 `estimated=1`、`worst=1`、`requires_confirmation=false`。
- `products get B0D8W1YVBX --domain US --rating 1 --buybox 1 --dry-run`：预算 `estimated=3`，components 为 `base_product/rating/buybox`。
- `audit cost products.get --param asin=B0D8W1YVBX --param offers=20`：预算 `estimated=13`，components 为 `base_product=1` 与 `offers=12`，`requires_confirmation=true`。

### 验证命令

- `.\.venv\Scripts\python.exe -m unittest tests.test_token_budget tests.test_service_commands -v`：21 tests passed。
- `.\.venv\Scripts\python.exe -m unittest tests.test_schema_snapshot -v`：1 test passed。

### 风险与后续

- `rating` 和 `buybox` 的附加成本按本项目保守预算模型估算；真实响应仍应以 Keepa 返回的 `tokensConsumed` 做最终审计。
- `offers=20` 现在按两页 offer 估算为 12 个附加 token，加基础产品成本为 13；这比旧 `next_actions` 示例中的 6 更保守，也更符合“每页最多 10 offers、每页 6 tokens”的本地官方摘记。

## 2026-05-10 继续优化：全历史窗口与增强时序特征

用户要求 history 窗口可设置，并默认选择官方可接受的最大值，同时继续增强 Agent 可用的时序特征。本轮保留单 ASIN 真实请求边界，不跑批量。

### 变更内容

- `--full` 默认从 `stats=180` 调整为 `stats=0`，作为全历史/最大 stats 窗口；新增 `--stats-window <days>` 允许调用方收窄窗口。
- `products get/compare` 新增 `--temporal-window-days`，可重复或传逗号列表；默认窗口为 7、30、90、180、365 天。
- `temporal_features` 继续从原始 `csv` 全量序列计算，不受 `--history-limit` 展示点数限制。
- 每个时序序列新增快速 O(n) 摘要：采样密度、动态窗口集合、median、latest_percentile、latest_zscore、IQR、MAD、方向变化次数、平均/最大步长、MAD 离群点、最大回撤/反弹、最长上升/下降段。
- schema 版本提升到 `2026-05-10.5`，同步 snapshot 与 `docs/schema/products.agent-view.schema.json`。

### 真实验证

命令：

```powershell
.\.venv\Scripts\python.exe -m keepa_cli --json products get B0D8W1YVBX --domain US --full --agent-view --view research --history-limit 12 --temporal-window-days 7 --temporal-window-days 30 --temporal-window-days 90 --temporal-window-days 180 --temporal-window-days 365 --out evidence/runtime-logs/20260510-B0D8W1YVBX-agent-temporal-v5.json
```

结果：

- 请求成功，`tokens_consumed=1`。
- 请求参数为 `history=1`、`stats=0`、`videos=1`、`aplus=1`，未自动请求 `rating=1` 或 `offers`。
- raw body 已写入 `evidence/runtime-logs/20260510-B0D8W1YVBX-agent-temporal-v5.json`，文件约 327KB，包含 1 个 product 与 36 个 csv series。
- Agent 视图中 `temporal_features.windows_days=[7,30,90,180,365]`；`new` 序列输出 129 点、`sales_rank` 输出 2737 点，并包含新增 sampling、dispersion、change_profile、outliers、shape 等特征。

### 风险与边界

- `evidence/runtime-logs/` 是真实响应日志，保持未跟踪，不纳入 git commit；如需长期 fixture，应先走 cassette 脱敏流程。
- `stats=0` 语义按 Keepa/Python wrapper 对 stats 正整数窗口与 date interval 的说明，以及本项目“最大窗口”需求处理；如官方后续明确不同最大值，需要同步调整 `--stats-window` 默认。

## 2026-05-10 继续优化：Agent 第一屏与证据索引

用户反馈 Agent 看到的信息“有些多有些杂，不够全面”，本轮不再继续堆叠 raw 字段，而是新增面向 Agent 消费顺序的结构层。

### 变更内容

- 每个产品新增 `agent_brief`：包含 `read_order`、`one_line`、`key_facts`、`temporal_takeaways`、`risk_flags`、`missing_data`、`confidence` 与 `recommended_next_actions`。
- 每个产品新增 `evidence_index`：为 `pricing.current`、`pricing.buy_box`、`demand`、`rating`、`selection_signals`、`temporal_features`、`history_summary`、`data_quality` 等深层证据提供稳定 JSON path、建议 section、`--view` 加载提示与用途说明。
- `summary` profile 改为优先返回 `agent_brief` 与核心事实，减少批量预筛上下文；`deal` 与 `audit` profile 增加 `evidence_index`。
- `--chunks-dir` 增加 `agent_brief` 与 `evidence_index` chunk，便于 Agent 按需加载。
- `next_actions` 中 `--offers 20` 的估算从旧的 6 修正为 13，与当前产品 token budget 模型一致。
- schema 版本提升到 `2026-05-10.6`，同步 snapshot 与 `docs/schema/products.agent-view.schema.json`。

### 验证命令

- `.\.venv\Scripts\python.exe -m unittest tests.test_service_commands -v`：15 tests passed。

### 风险与边界

- 本轮只用 fixture 验证契约结构，未再次访问真实 Keepa API，避免额外消耗 token。
- `agent_brief` 是派生摘要，不替代 `selection_signals`、`temporal_features` 和 raw output；需要审计时仍应通过 `evidence_index` 或 `--chunks-dir` 加载深层证据。
