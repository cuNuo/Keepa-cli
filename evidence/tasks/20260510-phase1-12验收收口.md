# Phase 1-12 验收收口

## 任务时间

- 开始时间：2026-05-10 15:00
- 最近更新时间：2026-05-10 15:40
- 当前状态：已完成

## 任务目标

- 按用户澄清范围核查 `docs/roadmaps/2026-05-09-keepa-cli-development-roadmap.md` 中 Phase 1 到 Phase 12 是否按量完成并通过验收。
- 对已具备底层能力但缺少正式 CLI 入口的计划命令做收口。

## 核查结论

- Phase 1-12 的离线优先 CLI 能力、Agent 协议、TUI、本地工作流、Tracking/webhook、发布门禁、MCP Agent tools 与文档入口已通过本地验收。
- Phase 12 中 `GitHub Release` 属于真实发布动作；本轮只读查询 `gh release list --limit 5` 未返回 release，因此严格按字面不能算已执行发布。
- 真实 Keepa live smoke 只能在显式 token/secret 下运行；项目已存在 `.github/workflows/live-keepa-smoke.yml` 和真实低成本 smoke evidence，但默认验收仍不消耗 token。

## 本轮补齐

- 新增正式 CLI 入口：
  - `products by-code`
  - `products summary`
  - `cache stats`
  - `cache clear --dry-run`
- `products by-code` 与 `products summary` 复用 `products.get`，不新增业务分叉。
- `cache stats/clear` 明确当前缓存后端为响应 provenance 与 Agent 进程内 session cache，没有默认磁盘响应缓存。
- 更新 `capabilities` schema 到 `2026-05-10.10`，并补 `cache.stats` / `cache.clear`。
- 更新 README / README.zh-CN 和 Agent schema snapshot。

## 验证结果

- `.\\.venv\\Scripts\\python.exe -m unittest discover -s tests -v`：183 tests OK。
- `git diff --check`：通过。
- `.\\.venv\\Scripts\\python.exe scripts\\install_verify.py --skip-npm-pack`：通过。
- `.\\.venv\\Scripts\\python.exe scripts\\release_gate.py --skip-npm-install`：通过，包含 compileall、全量测试、fixture sync、install verify、Python/Node doctor smoke、npm pack dry-run。
- `.\\.venv\\Scripts\\python.exe D:\\.codex\\hooks\\run_relevant_hooks.py --changed-only`：通过。
- CLI smoke：
  - `products by-code 9780786222728 --domain US --code-limit 5 --dry-run` 通过。
  - `products summary B0TESTAGENT --domain US --fixture product_agent_view_B0TEST.json` 通过。
  - `cache stats` 与 `cache clear --dry-run` 通过。

## 风险与后续

- 实际 GitHub Release 尚未创建；发布前应先确认版本号、tag、release notes、npm/PyPI 策略，再执行高副作用发布动作。
- `cache stats/clear` 是兼容路线图的状态入口，不等同于已实现持久磁盘响应缓存。
- 当前仍应继续拆分 `service.py` / `cli.py` 的大型命令族，并补 Agent evaluation fixtures。

## 结论

Phase 1-12 的可执行 CLI/Agent/TUI/发布门禁链路已完成本地验收；唯一不能标记为“已执行”的是 Phase 12 的真实 GitHub Release 发布动作。
