# MCP 无前缀契约文档同步

## 前置说明

- 时间：2026-05-12 12:03 +08:00。
- 范围：README、中文 README、Agent contract、SDK adapter 对照、MCP JSON Schema evidence 与 Serena memory。
- 目标：按 neat-freak 收口，确保外部客户端迁移只使用无前缀新名，并把 SDK adapter 生产入口前置 parity 条件写清楚。

## 变更

- 修正 `README.zh-CN.md` 中仍写 `keepa.*` 工具名不变、示例仍调用旧名的问题。
- `README.md` / `README.zh-CN.md` 明确外部客户端只迁移到 `context_policy`、`research_graph_merge`、`research_brief_export` 等无前缀新名，旧 `keepa.*` 不作为 alias 保留。
- `docs/agent-contract.md` 明确外部客户端迁移只改调用名，不提供旧名兼容映射。
- `docs/architecture/mcp-python-sdk-adapter-comparison.md` 明确 SDK adapter 提升生产入口前必须补齐 `toolset/profile/allow_tools/exclude_tools/limit/cursor` 过滤与分页 parity。
- 更新 `mcp_schema_validator_no_prefix_contract_20260512` Serena memory，避免下次会话误判为可恢复旧名前缀。

## 验证

- `rg` 检查当前面向接入的 README / docs 中不再残留旧工具名示例；仅保留 `keepa://` resource URI、`x-keepa` 元数据和用于说明旧名拒绝的 `keepa.context_policy` 负向记录。
- `git diff --check`：通过。
- `D:\.codex\hooks\run_relevant_hooks.py --changed-only`：通过。

## 风险与后续

- 不新增 `keepa.*` alias；若外部客户端仍硬编码旧名，应按文档迁移到新名。
- SDK adapter 未补齐过滤与分页 parity 前，继续保留 `python -m keepa_cli --mcp` raw stdio 作为唯一生产入口。
