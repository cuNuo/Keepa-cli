# 后端边界覆盖与 CI 收口

## 前置说明

- 本轮目标是整理当前未提交测试资产，提交并推送，同时确保本地门禁与 GitHub Actions 通过。
- 未访问真实 Keepa API，未消耗真实 token；所有新增覆盖均使用 fixture、dry-run、fake opener、临时目录或本地 service envelope。
- `coverage-groups.txt` 是本地覆盖率缺口分析中间产物，不具备稳定回归价值，已加入 `.gitignore`，不纳入提交。

## 已落地

1. 客户端边界覆盖
   - 扩展 `tests/test_client.py`，覆盖无 API key 时阻断网络、binary 请求必须显式 output path、fixture 错误结构化、POST JSON body、网络错误重试、无效 JSON、429 refill 解析、binary 写文件、HTTP body fallback 与 secret 脱敏。

2. 后端覆盖率回归资产
   - 新增 `tests/test_backend_branch_coverage.py`、`tests/test_backend_edge_coverage.py`、`tests/test_backend_transform_coverage.py`、`tests/test_backend_remaining_coverage.py`、`tests/test_backend_final_coverage.py`。
   - 覆盖 MCP/stdio/session、resources、workflow resolver、figures、product_view、research graph、research brief、cassette、tracking、cache、category/finder/deals/seller 等边界路径。

3. 提交边界整理
   - `.gitignore` 新增 `coverage-groups.txt`，防止一次性覆盖率分析报告误入仓库。
   - 保留可由 `unittest discover` 自动发现的测试文件作为长期离线回归资产。
   - 清理 `workflow_resolver` 中无实际来源的 `temp_path` 旧分支；运行期临时输入路径仍由 `_apply_derived_params` 统一写入 `temp_paths`，并由现有 MCP/workflow 测试覆盖。

## 已执行验证

- `.\.venv\Scripts\python.exe -m unittest discover -s tests -v`：300 tests OK。
- `.\.venv\Scripts\python.exe scripts\check_fixture_sync.py`：OK。
- `.\.venv\Scripts\python.exe scripts\check_agent_eval_fixtures.py`：29 specs OK。
- `.\.venv\Scripts\python.exe -m compileall -q keepa_cli scripts`：OK。
- `git diff --check`：OK。
- `.\.venv\Scripts\python.exe D:\.codex\hooks\run_relevant_hooks.py --changed-only`：OK。
- `.\.venv\Scripts\python.exe -m keepa_cli --json doctor`：OK。
- `node .\bin\keepa-cli.js --json doctor`：OK。
- `node .\bin\kc.js --json doctor`：OK。
- `npm pack --dry-run --json`：OK；prepack release gate OK。

## 风险与后续

- 新增测试偏覆盖率补洞，后续仍应把高价值断言逐步迁移为按功能主题命名的可读规格测试，降低长期维护成本。
- `service.py`、`product_view.py`、`figures.py`、`agent/resources.py` 仍是高复杂模块，下一轮最适合继续拆分命令族与图表渲染 helper，并保留当前边界测试作为重构护栏。

## 2026-05-11 CI 路径断言修复

- 首次推送 `b318db3` 后，GitHub Actions run `25656483433` 在全部平台的 `Run unit tests` 失败。
- 根因：新增测试直接比较临时文件路径字符串；GitHub Windows runner 会把同一用户目录表示为 `runneradmin` 或 `RUNNER~1`，导致字符串断言失败。
- 修复：将相关断言改为 `Path(...).resolve()` 后比较真实路径，并补充 `test_backend_final_coverage.py` 中本地资源/evidence 空态与只读 tracking dry-run 的离线覆盖。
- 修复后已重新执行：
  - `.\.venv\Scripts\python.exe -m unittest tests.test_backend_remaining_coverage tests.test_backend_transform_coverage tests.test_backend_final_coverage -v`：19 tests OK。
  - `.\.venv\Scripts\python.exe -m unittest discover -s tests -v`：300 tests OK。
  - `.\.venv\Scripts\python.exe scripts\check_fixture_sync.py`：OK。
  - `.\.venv\Scripts\python.exe scripts\check_agent_eval_fixtures.py`：29 specs OK。
  - `.\.venv\Scripts\python.exe -m compileall -q keepa_cli scripts`：OK。
  - `git diff --check`：OK。
  - `.\.venv\Scripts\python.exe D:\.codex\hooks\run_relevant_hooks.py --changed-only`：OK。
